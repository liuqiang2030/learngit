package {		import flash.display.MovieClip;	import flash.text.*;	import flash.events.*;	import flash.ui.*;	/** 	*	Main Class	*	@ purpose:		Document class for movie	*/	public class Main extends MovieClip {				private var carAdapter; // declare adapter (no type due to MC coersion error)		public function Main() {			// show instructions			var t:TextField = new TextField();			var f:TextFormat = new TextFormat();			f.size = 12;			f.font = "Arial";			f.align = TextFormatAlign.CENTER;			t.width = 540;			t.x = 5;			t.y = 20;			t.text = "To turn steering wheel press LEFT and RIGHT arrow keys. "			t.text += "UP arrow key straightens steering";			t.setTextFormat(f);			addChild(t);						// Instantiate CarAdapter			this.carAdapter = new CarAdapter(this.stage.stageWidth * 0.5, 											 this.stage.stageHeight * 0.75);						addChild(this.carAdapter); // add adapter to display list of stage						// attach KEY_DOWN event listener onKeyPress() to the stage			this.stage.addEventListener(KeyboardEvent.KEY_DOWN, this.onKeyPress);						carAdapter.start();		}				private function onKeyPress(event:KeyboardEvent):void {			switch (event.keyCode) {				case Keyboard.LEFT : 					this.carAdapter.turnMoreToTheLeft();					break;				case Keyboard.RIGHT : 					this.carAdapter.turnMoreToTheRight();					break;				case Keyboard.UP : 					this.carAdapter.goStraight();					break;			}		}	} }