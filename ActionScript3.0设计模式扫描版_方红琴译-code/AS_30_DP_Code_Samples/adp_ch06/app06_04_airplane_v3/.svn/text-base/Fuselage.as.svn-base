package {		import flash.events.*;		public class Fuselage extends Composite {				public function Fuselage(weight:Number, damage:Number = 0) {			graphics.lineStyle(40, 0xC0C0C0);			graphics.moveTo(0, 0);			graphics.lineTo(0, 150);						addEventListener(MouseEvent.CLICK, doDamage);						super(weight, damage);		}				private function doDamage(evt:Event) {			this.nDamage += 10;			trace('Damage to the fuselage is now ' + this.nDamage);			evt.stopPropagation(); // stop event propegation to subsequent nodes		}	}}