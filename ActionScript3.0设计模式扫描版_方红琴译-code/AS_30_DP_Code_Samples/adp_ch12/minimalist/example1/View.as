package {		import flash.events.*;	import flash.display.*;	public class View {				private var model:Object;		private var controller:Object;				public function View(oModel:Object, oController:Object, target:Stage) {			this.model = oModel;			this.controller = oController;			// register to receive notifications from the model			model.addEventListener(Event.CHANGE, this.update); 						// register to receive key press notifications			target.addEventListener(KeyboardEvent.KEY_DOWN, this.onKeyPress);		}				private function update(event:Event):void {			// get data from model and update view			trace(model.getKey());		}				internal function onKeyPress(event:KeyboardEvent):void {			// delegate to the controller to handle it			controller.keyPressHandler(event);		}	}}